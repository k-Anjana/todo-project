version:2.1

workflows:
  test-deploy:
    jobs:
      -test

jobs:
  build:
    docker:
      - image: cimg/python:3.12.1
    steps:
      - checkout
      - run: python --version
         name: Install dependencies
         command: pip install
        - run: 
           name: Run Tests
           command: pip run test
         
